---
title: Your First App
description: Build your first Cinder application
icon: rocket
---

In this guide, you'll build an interactive counter app. You'll learn how to create components, manage state, and handle keyboard input.

## Create the app

Create a file called `counter.dart` and add this code:

```dart
import 'package:cinder/cinder.dart';

void main() {
  runApp(const Counter());
}

class Counter extends StatefulWidget {
  const Counter({super.key});

  @override
  State<Counter> createState() => _CounterState();
}

class _CounterState extends State<Counter> {
  int _count = 0;

  @override
  Widget build(BuildContext context) {
    return Focusable(
      focused: true,
      onKeyEvent: (event) {
        if (event.logicalKey == LogicalKey.space) {
          setState(() => _count++);
          return true;
        }
        return false;
      },
      child: Container(
        decoration: BoxDecoration(
          border: BoxBorder.all(color: Colors.gray),
        ),
        margin: EdgeInsets.all(8),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text('Counter: $_count'),
            SizedBox(height: 1),
            Text('Press SPACE to increment', style: TextStyle(color: Colors.gray)),
          ],
        ),
      ),
    );
  }
}
```

Run the app:

```bash
dart run counter.dart
```

Press the spacebar to increment the counter. Press `Ctrl+C` to exit.

## How it works

Let's break down each part of the code.

### runApp

The `runApp()` function starts your app:

```dart
void main() {
  runApp(const Counter());
}
```

This function takes your root widget and displays it in the terminal. It sets up the terminal (enables raw mode, alternate screen), handles rendering, and processes input events.

### StatefulWidget

`Counter` extends `StatefulWidget` because it needs to track the count:

```dart
class Counter extends StatefulWidget {
  const Counter({super.key});

  @override
  State<Counter> createState() => _CounterState();
}
```

A `StatefulWidget` separates the widget (which is immutable) from its state (which changes over time). The `createState()` method creates the state object.

### State and setState

The `_CounterState` class holds the count and rebuilds the UI when it changes:

```dart
class _CounterState extends State<Counter> {
  int _count = 0;

  @override
  Widget build(BuildContext context) {
    // ... UI code
  }
}
```

When you call `setState()`, Cinder rebuilds the widget with the new state:

```dart
setState(() => _count++);
```

Always wrap state changes in `setState()`. This tells Cinder to redraw the UI.

### The build method

The `build()` method returns the UI for your widget:

```dart
Widget build(BuildContext context) {
  return Focusable(
    // ... widget tree
  );
}
```

Cinder calls `build()` when:
- The widget first appears
- You call `setState()`
- The parent widget rebuilds

### Handling keyboard input

The `Focusable` widget captures keyboard events:

```dart
Focusable(
  focused: true,
  onKeyEvent: (event) {
    if (event.logicalKey == LogicalKey.space) {
      setState(() => _count++);
      return true;  // Event handled
    }
    return false;  // Not handled, pass to next widget
  },
  child: // ...
)
```

The `onKeyEvent` callback receives each key press. Return `true` if you handled the event, or `false` to pass it along.

### Layout components

The UI uses layout components to arrange text:

```dart
Column(
  mainAxisAlignment: MainAxisAlignment.center,
  children: [
    Text('Counter: $_count'),
    SizedBox(height: 1),
    Text('Press SPACE to increment', style: TextStyle(color: Colors.gray)),
  ],
)
```

- `Column` stacks children vertically
- `mainAxisAlignment.center` centers them vertically
- `SizedBox` adds spacing between elements

## Try it yourself

Modify the app to:
- Increment on up arrow, decrement on down arrow
- Add a reset button (press `R` to reset to 0)
- Display the count in a different color when it's above 10
- Add a border that changes color based on the count

## Next steps

- [How terminals work](/docs/terminal-concepts/how-terminals-work) - Understand what's happening under the hood
- [Components](/docs/fundamentals/components) - Learn about StatelessWidget and StatefulWidget
- [Building UIs](/docs/fundamentals/building-uis) - Compose complex interfaces
