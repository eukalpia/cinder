# Human Design

Canonical source: `skills/human_design/SKILL.md`.

Before changing a public Cinder interface, read and follow the canonical skill. The design must show real Cinder source and runtime output, use terminal-native composition, avoid generic SaaS patterns, disclose browser/native limits, and pass the completion gate defined there.
