name: example
description: An interactive Cinder browser demonstration.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.9.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  cinder:
    path: ../../..

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.6
